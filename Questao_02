#include <stdio.h>

int main() {
    int x = 10,y=4,temp = 0;
    int *a = &x;
    int *b = &y;
    printf("Antes x: %d, y: %d\n",x,y);
    temp = *a;
    *a = *b;
    *b = temp;
    printf("Depois x: %d, y: %d",x,y);
    return 0;
}
